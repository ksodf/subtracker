# SubTracker Mobile

Expo React Native frontend for the existing SubTracker Render API.

## Setup

```sh
npm install
npx expo start
```

Open the QR code in Expo Go on iOS or Android.

## API URL

Change the backend API URL in `src/config/api.js`.
